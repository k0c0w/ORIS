import CustomButton from "../components/CustomButton"

const GroupActionButton = ({value, onClick}) => {
    return <CustomButton value={"text"} onClick = {()=> alert("text")}></CustomButton>
}

export default GroupActionButton;
